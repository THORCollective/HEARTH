# H285

Threat actors are embedding malicious build.rs scripts in Rust crate dependencies that decode base64-fragmented URLs, disable TLS certificate validation via a custom AcceptAllServerCertVerifier, and silently fetch second-stage binaries during the `cargo build` compile-time process to achieve pre-execution code execution on developer and CI machines without any application code ever running.

| Hunt #       | Idea / Hypothesis                                                      | Tactic         | Notes                                                                              | Tags                           | Submitter           |
|--------------|-------------------------------------------------------------------------|----------------|------------------------------------------------------------------------------------|--------------------------------|---------------------|
| H285 | Threat actors are embedding malicious build.rs scripts in Rust crate dependencies that decode base64-fragmented URLs, disable TLS certificate validation via a custom AcceptAllServerCertVerifier, and silently fetch second-stage binaries during the `cargo build` compile-time process to achieve pre-execution code execution on developer and CI machines without any application code ever running. | Execution | Based on ATT&CK technique T1195.002 (Compromise Software Supply Chain / Compromise Software Dependencies and Development Tools). Generated by [hearth-auto-intel](https://github.com/THORCollective/HEARTH). | #execution #T1195.002 #rust #supplychain #cargo #buildscript | [DejaWh0](_No response_) |

## Why
- Rust build scripts (`build.rs`) execute automatically at compile time with the full privileges of the building user, before any application logic runs and before `cargo audit` or dependency scanners can meaningfully inspect behavior, making this an especially stealthy and high-trust execution vector.
- A single `cargo build` or `cargo update` on a poisoned lockfile is sufficient to trigger payload download and execution; the compromised crate's own source code is never called, meaning traditional "does this library get invoked" review misses the threat entirely.
- The technique bypasses supply-chain hygiene controls that focus on published package code rather than transitive build-time dependencies, and the detached process spawn with `std::mem::forget(child)` allows the payload to persist and communicate with C2 after the build step exits successfully, leaving CI logs and Actions UI green.
- This pattern (build-time dropper as a typosquatted "helper" dependency) is directly reusable against any language ecosystem with build hooks (npm postinstall, Python setup.py, Rust build.rs, Go generate), so detecting it in Rust telemetry now hardens hunting playbooks for future ecosystem-agnostic supply-chain campaigns.
- Rotating credentials and rebuilding artifacts is only effective if the initial build-time execution is detected quickly; egress monitoring for anomalous outbound connections during build steps is the most reliable way to catch this class of attack before secrets are exfiltrated.

## References
- [MITRE ATT&CK T1195.002 – Compromise Software Supply Chain](https://attack.mitre.org/techniques/T1195/002/)
- [Source CTI Report](https://www.stepsecurity.io/blog/arrayref-rust-crate-supply-chain-attack)