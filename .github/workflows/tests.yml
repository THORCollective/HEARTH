name: Test Suite

on:
  push:
    branches: [ main, develop, refactor/* ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11"]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'

    - name: Install dependencies
      run: |
        echo "::group::Install dependencies"
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        echo "::notice::Dependencies installed successfully"
        echo "::endgroup::"

    - name: Run unit tests
      run: |
        echo "::group::Run unit tests"
        pytest tests/unit/ -v
        echo "::endgroup::"

    - name: Run integration tests
      run: |
        echo "::group::Run integration tests"
        pytest tests/integration/ -v
        echo "::endgroup::"

    - name: Run all tests with coverage
      run: |
        echo "::group::Run all tests with coverage"
        pytest --cov=scripts --cov-report=xml --cov-report=term tests/
        echo "::endgroup::"

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

  lint:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"
        cache: 'pip'

    - name: Install dependencies
      run: |
        echo "::group::Install linting dependencies"
        python -m pip install --upgrade pip
        pip install flake8 pylint
        echo "::notice::Linting dependencies installed successfully"
        echo "::endgroup::"

    - name: Lint with flake8
      run: |
        echo "::group::Lint with flake8"
        # Stop the build if there are Python syntax errors or undefined names
        flake8 scripts/ --count --select=E9,F63,F7,F82 --show-source --statistics
        # Exit-zero treats all errors as warnings
        flake8 scripts/ --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
        echo "::endgroup::"
      continue-on-error: true
